FROM alpine:3.20

RUN apk add --no-cache \
      build-base \
      bash \
      git \
      rust \
      cargo \
      zstd

WORKDIR /workspace

CMD ["bash", "-lc", "cd /workspace && ./scripts/build-alpine-apk.sh"]

